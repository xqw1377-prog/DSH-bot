[project]
name = "dsh-contracts"
version = "0.1.0"
description = "DSH Bot 领域对象契约（Pydantic 模型）"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7,<3"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
