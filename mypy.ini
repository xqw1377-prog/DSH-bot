# mypy 首批接入:先覆盖领域契约包(所有服务的共同依赖)。
# 逐步扩展到其余包;宽松基线,不做严格模式。
[mypy]
python_version = 3.12
files = packages/domain-contracts/src
ignore_missing_imports = True
check_untyped_defs = True
warn_redundant_casts = True
no_implicit_optional = True
